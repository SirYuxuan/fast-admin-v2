<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cc.oofo.system.permission.mapper.SysRolesMenusMapper">

    <!-- 批量插入角色菜单关联 -->
    <insert id="batchInsert" parameterType="java.util.List"> INSERT INTO sys_roles_menus (role_id,
        menu_id) VALUES <foreach collection="list" item="item" separator=","> (#{item.roleId},
        #{item.menuId}) </foreach>
    </insert>

</mapper>